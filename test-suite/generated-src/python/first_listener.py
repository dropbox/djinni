# AUTOGENERATED FILE - DO NOT MODIFY!
# This file generated by Djinni from multiple_inheritance.djinni

from djinni.support import MultiSet # default imported in all files
from djinni.exception import CPyException # default imported in all files


from abc import ABCMeta, abstractmethod
from future.utils import with_metaclass
from PyCFFIlib_cffi import ffi, lib

from djinni import exception # this forces run of __init__.py which gives cpp option to call back into py to create exception

class FirstListener(with_metaclass(ABCMeta)):
    """ Used for non-C++ multiple inheritance tests """

    @abstractmethod
    def first(self):
        raise NotImplementedError


class FirstListenerCallbacksHelper():
    @ffi.callback("void(struct DjinniObjectHandle * )")
    def first(cself):
        try:
            FirstListenerHelper.selfToPy(cself).first()
        except Exception as _djinni_py_e:
            CPyException.setExceptionFromPy(_djinni_py_e)

    @ffi.callback("void(struct DjinniObjectHandle * )")
    def __delete(c_ptr):
        assert c_ptr in FirstListenerHelper.c_data_set
        FirstListenerHelper.c_data_set.remove(c_ptr)

    @staticmethod
    def _add_callbacks():
        lib.first_listener_add_callback_first(FirstListenerCallbacksHelper.first)

        lib.first_listener_add_callback___delete(FirstListenerCallbacksHelper.__delete)

FirstListenerCallbacksHelper._add_callbacks()

class FirstListenerHelper:
    c_data_set = MultiSet()
    @staticmethod
    def toPy(obj):
        if obj == ffi.NULL:
            return None
        # Python Objects can be returned without being wrapped in proxies
        py_handle = lib.get_handle_from_proxy_object_cw__first_listener(obj)
        if py_handle:
            assert py_handle in FirstListenerHelper.c_data_set
            aux = ffi.from_handle(ffi.cast("void * ", py_handle))
            lib.first_listener___wrapper_dec_ref(obj)
            return aux
        return FirstListenerCppProxy(obj)

    @staticmethod
    def selfToPy(obj):
        assert obj in FirstListenerHelper.c_data_set
        return ffi.from_handle(ffi.cast("void * ",obj))

    @staticmethod
    def fromPy(py_obj):
        if py_obj is None:
            return ffi.NULL
        py_proxy = (py_obj)
        if not hasattr(py_obj, "first"):
            raise TypeError

        bare_c_ptr = ffi.new_handle(py_proxy)
        FirstListenerHelper.c_data_set.add(bare_c_ptr)
        wrapped_c_ptr = lib.make_proxy_object_from_handle_cw__first_listener(bare_c_ptr)
        return wrapped_c_ptr
